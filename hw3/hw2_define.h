#ifndef _HW2_DEFINE_H_
#define _HW2_DEFINE_H_

namespace csci561
{

//#define HW2_DEBUG

const int MAX_INFT = 100;
const int MIN_INFT = -100;

#define MAX(a, b) (((a)>(b)) ? (a) : (b))
#define MIN(a, b) (((a)<(b)) ? (a) : (b))

} //namespac

#endif
